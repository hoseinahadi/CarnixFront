"use client";
import {FormEvent,useEffect,useState} from 'react';import {useAuth} from '@/components/AuthProvider';import {API_BASE} from '@/lib/api';
export default function Login(){const{login,user,loading}=useAuth();const[u,setU]=useState(''),[p,setP]=useState(''),[remember,setRemember]=useState(true),[err,setErr]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{if(user)location.href='/admin/dashboard'},[user]);const submit=async(e:FormEvent)=>{e.preventDefault();setErr('');setBusy(true);try{await login(u,p,remember)}catch(e:any){setErr(e.message)}finally{setBusy(false)}};return <main className="login-page"><section className="login-art"><div className="login-brand"><div className="brand-mark big">C</div><div><strong>Carnix</strong><span>Administration System</span></div></div><div className="hero-copy"><span>ADMIN CONSOLE / 01</span><h1>کنترل کامل فروشگاه،<br/>از یک نقطه.</h1><p>محصول، سفارش، انبار، کاربران، دسترسی‌ها، محتوا و همه APIهای مدیریتی Carnix در یک پنل RTL.</p></div><div className="art-grid"><i/><i/><i/><i/></div></section><section className="login-form-wrap"><form onSubmit={submit} className="login-form"><div><span className="eyebrow">ورود امن</span><h2>ورود به پنل مدیریت</h2><p>با حساب دارای سطح دسترسی مدیریتی وارد شوید.</p></div><label><span>نام کاربری</span><input autoFocus autoComplete="username" value={u} onChange={e=>setU(e.target.value)} required/></label><label><span>رمز عبور</span><input type="password" autoComplete="current-password" value={p} onChange={e=>setP(e.target.value)} required/></label><label className="remember"><input type="checkbox" checked={remember} onChange={e=>setRemember(e.target.checked)}/><span>توکن ورود روی این دستگاه نگهداری شود</span></label>{err&&<div className="login-error">{err}</div>}<button className="primary login-button" disabled={busy||loading}>{busy?'در حال ورود...':'ورود به پنل'}</button><div className="api-note"><span className="connection-dot"/><div><b>API Endpoint</b><small>{API_BASE}</small></div></div></form></section></main>}
